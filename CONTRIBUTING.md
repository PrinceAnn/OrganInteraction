# Contributing

Keep contributions source-agnostic and test them with artificial data. Pull
requests must pass both the unit tests and `scripts/quality/audit_release.py`.

Do not include real source column names in code, tests, commits, branch names,
issues, or pull-request descriptions. Put source adapters and private
configuration in access-controlled repositories instead.
